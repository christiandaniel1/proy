git init
git add
git add .
git commit -m "ddd"

git branch
git branch Nombre
cambiar rama:
git checkout master
git status
-----

git hub

creo repositorio

---
-llevar local a github:
creo repositorio en github
copio 2 enlaces dado por repositorio:
1 git remote add origin https://github.com/....
2- push -u origin master

local agrego archivo(usando add, commit) etc ->hago push se va reflejar gitHub?

-De gitHub a local :
git clone https://github.com/AxelJean/ProyectoEjemplo.git

probar local(usuario1) a github y githut llevarlo local(usuario2)